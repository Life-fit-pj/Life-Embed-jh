# 프론트엔드 요청/확인 사항 — 시세(집값) 반영 관련

추천 엔진(`Life-Embed-jh`)에 시세 데이터를 반영하는 작업을 진행 중입니다(설계 내용은 `STUDY.md`
참고). 화면에 이미 있는 UI(건물유형 드롭다운, 거래유형 탭, 보증금/면적 슬라이더, 주변 시세 탭)와
엔진을 연결하려면 아래 사항이 필요합니다.

---

## 1. [신규 요청] "가격 상관없음" 체크박스 추가

**왜 필요한가**: 지금 슬라이더 UI만으로는 "가격대를 최소로 맞췄다"와 "가격은 아예 안 봐도
된다"를 구분할 수 없습니다. 슬라이더를 최소로 내려도 그건 여전히 "이 가격대를 원한다"는 뜻으로
처리되기 때문에(엔진이 슬라이더 값을 목표가로 보고 "가까운 동네"를 찾습니다 — 2번 참고), 가격
조건을 아예 안 쓰고 싶은 사용자를 위한 별도 체크박스(또는 토글)가 필요합니다.

- **배치 제안**: 예산/보증금 슬라이더 옆 (예: "가격 상관없음" 체크박스)
- **체크했을 때 프론트가 할 일**: 건물유형·거래유형·예산·보증금 값을 요청에서 아예 빼거나
  `null`로 보내면 됩니다. 엔진이 이 값들이 비어있으면 가격 필터를 완전히 건너뛰고 지금처럼
  7개 지표(녹지·안전·교통·상권·의료·교육·문화)만으로 추천합니다.
- 슬라이더는 체크박스가 꺼져 있을 때만 활성화되는 게 자연스러워 보입니다(막아두지 않으면
  "체크했는데 슬라이더 값이 남아있으면 어느 게 우선인지" 헷갈릴 수 있음).

---

## 2. [확인 필요] 가격 슬라이더는 "상한선"이 아니라 "목표가"로 처리됩니다

엔진 쪽에서 이미 정한 사항이라 프론트 문구를 맞춰주셔야 합니다.

- 슬라이더로 지정한 값을 **"이 가격 이하면 다 좋다"가 아니라 "이 가격에 가까울수록 좋다"**로
  계산합니다. 목표가보다 훨씬 싼 매물도, 훨씬 비싼 매물도 똑같이 감점됩니다.
  - 예: "전세 보증금 2억 3,000만"으로 맞추면, 8천만원짜리 동네가 아니라 2억 3천에 가까운
    동네가 더 높은 점수를 받습니다.
  - 이유: 강남처럼 비싼 동네에서도 "그중 저렴한 곳"이 아니라 "신축·고가 매물"을 일부러 찾는
    사용자도 있기 때문입니다.
- **요청**: 슬라이더 라벨이 "예산"·"최대 보증금"처럼 상한선을 암시하는 문구라면, "희망
  가격대"·"원하는 보증금" 같은 목표값 뉘앙스로 바꿔주시면 사용자 오해를 줄일 수 있습니다.
  (필수는 아니고, UX 관점에서 권장 — 최종 문구는 프론트 판단에 맡깁니다.)

---

## 3. [확인 필요] 월세는 "보증금"과 "월세" 두 값이 따로 필요합니다

- 월세 매물은 보증금(목돈)과 월세(매달 내는 돈)가 별개 금액이고, 엔진은 **월세 쪽을 더
  중요하게(약 70:30 비율로) 반영**하도록 설계했습니다.
- **확인 요청**: 거래유형을 "월세"로 선택했을 때, 화면에 보증금 입력과 월세 입력이 **둘 다**
  있는지 확인 부탁드립니다. 하나만 있다면 월세 입력 슬라이더/필드를 추가로 요청드립니다.
- 보증금은 선택 입력으로 둬도 됩니다 — 월세 값만 있어도 추천은 동작하고, 보증금까지 주면
  참고 점수 계산에 같이 반영됩니다.

---

## 4. [확인 필요] 건물유형 드롭다운의 실제 옵션값

- 엔진이 실제로 갖고 있는 데이터의 건물유형은 **`단독다가구` / `아파트` / `연립다세대` /
  `오피스텔`** 네 가지뿐입니다. "빌라"·"원룸" 같은 값은 원본 데이터에 없습니다.
- **확인 요청**: 프론트 드롭다운이 이 네 가지 그대로 쓰는지, 아니면 "빌라"·"원룸" 같은 다른
  표시 이름을 쓰는지 알려주세요.
  - 그대로 쓴다면 별도 작업 없이 그 값을 그대로 보내주시면 됩니다.
  - 다른 이름을 쓴다면, 프론트→엔진으로 보낼 때 어떤 이름을 어떤 값으로 바꿔 보낼지
    매핑표(예: "빌라" → "연립다세대", "원룸" → "오피스텔")를 같이 정해야 합니다. 엔진 쪽에서
    변환할 수도 있으니, 프론트가 원본 라벨 그대로 보내주셔도 무방합니다 — 어느 쪽이 변환을
    맡을지만 정하면 됩니다.

---

## 5. [결정 필요] 건축 면적 슬라이더는 지금 추천 로직에 반영되지 않습니다

- 현재 엔진이 갖고 있는 시세 데이터는 "동네 하나당 건물유형×거래유형 조합의 대표 시세"까지만
  있고, 면적 구간별로 세분화된 값은 추천 계산에 안 쓰고 있습니다(원본 데이터엔 면적구간별
  정보가 있지만, 지금 설계엔 안 엮여 있습니다).
- 두 가지 선택지가 있습니다.
  - **A. 지금은 안 보낸다** — 면적 슬라이더는 화면에 남겨두되(사용자가 참고용으로 가늠하는
    용도), 엔진 요청에는 안 실어도 됩니다. 별도 개발 없이 바로 가능합니다.
  - **B. 면적까지 반영한다** — 면적구간(20㎡이하/20-40/40-60/60-85/85-135/135초과) 단위로
    시세를 다시 붙이는 추가 작업이 필요합니다(개발 소요 있음, 지금 일정엔 없음).
- **요청**: 지금 단계에선 A로 가는 걸 제안드립니다. B가 필요하면 알려주시면 별도로 설계
  하겠습니다.

---

## 6. [논의 필요] "주변 시세" 탭에 보여줄 정보

화면에 "서울 427개 행정동 공공데이터 기준 백분위"라는 자리만 있고 아직 실제 값이 안 붙어있는
것으로 보입니다. 엔진이 줄 수 있는 값은 다음과 같습니다 — 어떤 조합으로 보여줄지 확인
부탁드립니다.

- 선택한(또는 기본) 건물유형·거래유형 기준으로:
  - 그 동네의 중앙값 금액 (예: "3억 8,000만원")
  - 25%~75% 분포 범위 (예: "3억 2천~4억 3천")
  - 서울 전체 동 대비 백분위 (예: "상위 30%" — **단, 시세는 낮다고 무조건 "좋다"가 아니므로**
    화면 문구를 "저렴한 편"이 아니라 "서울 전체 중 이 정도 위치"처럼 방향성 없이 표현하는 걸
    권장합니다)
- 건물유형·거래유형을 선택 안 한 상태(가격 상관없음 체크 등)에서도 이 탭을 보여줄지, 아니면
  그 경우엔 탭을 숨기거나 "조건을 선택하면 시세를 볼 수 있어요" 안내로 대체할지도 정해야
  합니다.

---

## 7. [참고] 자연어 검색에도 가격 조건이 자동으로 반영됩니다

별도 요청 사항은 아니지만 참고해주세요 — "전세 4억 정도로 조용한 동네" 같은 자연어 검색어를
그대로 보내도, 엔진이 검색어에서 거래유형·예산을 알아서 추출해 반영합니다. 이 경로는 프론트에서
별도 처리 없이 지금처럼 검색어만 보내면 됩니다. (단, "4억 이하로"처럼 명확한 상한선 표현과
"4억 정도"의 차이는 지금 구분하지 않고 둘 다 목표가로 처리됩니다 — 2번 항목과 같은 이슈입니다.)

---

## 요약 — 우선 확인해주셔야 할 것

| 번호 | 항목 | 종류 |
|---|---|---|
| 1 | "가격 상관없음" 체크박스 추가 | 신규 개발 요청 |
| 2 | 가격 슬라이더 = 목표가(상한선 아님) — 문구 확인 | 확인/권장 |
| 3 | 월세 선택 시 보증금+월세 두 입력 있는지 | 확인 (없으면 개발 요청) |
| 4 | 건물유형 드롭다운 실제 옵션값 | 확인 |
| 5 | 면적 슬라이더 반영 여부 (A: 안 보냄 / B: 추가 개발) | 결정 필요 |
| 6 | 주변 시세 탭에 보여줄 정보 형식 | 논의 필요 |
